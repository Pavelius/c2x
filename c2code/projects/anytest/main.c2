import geo.point;
import geo.rect;

public static char* string = "Count is good";
public static char t1[8] = {12, 8, 10};
public static char t2[] = {2, 4, 0};

int testmain(int a, int b)
{
	return a + b;
}

int main()
{
	int c = t1[3];
	int a = sizeof(rect);
	if(!a)
		return 0;
	int b = sizeof(point) + testmain(10, 2);
	while(a>0)
	{
		b = b + a - 2;
		a--;
	}
}